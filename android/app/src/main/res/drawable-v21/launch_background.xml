<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash for pre-Android-12. From API 31 the system SplashScreen API
     owns this moment instead — see values-v31/styles.xml. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <!-- Transparent logo over the navy field, so there is no icon plate.
         Drawn at its intrinsic size, which is why splash_logo.png is
         generated per density bucket (160dp) rather than scaled here. -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
