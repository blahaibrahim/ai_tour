<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="ic_launcher_background">#19274C</color>
    <!-- Splash / window background: the navy field the app icon sits on.
         Kept identical to ic_launcher_background above (and sampled from the
         artwork's own field) so the transparent splash logo reads as part of
         the same surface — at #14254A it was a few units off, which shows as a
         faint mismatch between the splash and the icon. -->
    <color name="splash_background">#19274C</color>
</resources>